/* eslint-disable */
import React from "react";
import Section from "./Section";

export default {
  title: "Common Components/Molecules/Section",
  component: Section,
  argTypes: {},
};

const Template = (args) => <Section {...args} />;
export const SectionComponent = Template.bind({});
